package Lecture_12;

//Update class Employee to be abstract.
//Rename salary field to base salary. Create a method to return the base salary.
//The method must be available only for all subclasses. Hint: use the most suitable access modifier
//Create an abstract method that calculates the monthly salary
//Implement concrete subclasses FullTimeEmployee and PartTimeEmployee that extend the Employee class and provide specific implementations for calculating the monthly salary
//The FullTimeEmployee monthly salary calculation must be base salary
//For the PartTimeEmployee add a fields hoursWorked and hourlyRate. PartTimeEmployee monthly salary must be calculated based on those two fields.
//Create objects of FullTimeEmployee and PartTimeEmployee and assign them the Employee variables.
//Print the name and monthly salary of each employee.

public class Exercise_03 {
}
